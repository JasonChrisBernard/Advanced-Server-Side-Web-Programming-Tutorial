<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>Search Results</title>
  <style>
    ul { list-style: none; padding: 0; max-width: 600px; margin: 2em auto; }
    li { padding: 0.5em 0; border-bottom: 1px solid #ddd; }
  </style>
</head>
<body>
  <%- include('../partials/menu') %>
  <h1>Search Results</h1>

  <!-- Show which field was used -->
  <p>
    <% if (query.genre) { %>
      Genre: <strong><%= query.genre %></strong>
    <% } else if (query.title) { %>
      Title contains: <strong><%= query.title %></strong>
    <% } %>
  </p>

  <% if (!movies || movies.length === 0) { %>
    <p>No movies found.</p>
  <% } else { %>
    <ul>
      <% movies.forEach(m => { %>
        <li>
          <strong><%= m.title %></strong>
          (<%= m.release_year %>) – directed by <%= m.director %>,
          genre: <%= m.genre %>, rating: <%= m.imdb_rating %>
        </li>
      <% }) %>
    </ul>
  <% } %>
</body>
</html>
